package ErdemsMentoring;

public class Car {
	  
/*  String model;
  double engine = 3.5;
  
  public Car (String model, double engine) {
    Car(model);//line 1 - compile error because you can call a constructor by this() keyword in the same class
    setEngine(engine);
  }
  
  
  public Car (String model) {
    this();//line 2//compiler does not provide a no-arg constructor because we have constructors already
    setModel(model);
  }
  
  public String print() {
    return model + ", " + engine;
  }
  
  //getters and setters
  
  public static void main(String[] args) {
    Car car1 = new Car("Impreza");
    Car car2 = new Car("RX-8", 4.2);
    System.out.println(car1.print());
    System.out.println(car2.print());
  }
*/
  
}
